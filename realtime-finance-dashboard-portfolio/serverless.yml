service: realtime-finance-dashboard
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1
  stage: dev
  httpApi:
    cors: true

functions:
  getQuotes:
    handler: backend/lambda.getQuotes
    events:
      - httpApi:
          path: /quotes
          method: get